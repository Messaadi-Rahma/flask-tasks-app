apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flask-ingress
spec:
  rules:
  - host: flask-tasks.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: flask-tasks-service
            port:
              number: 5000
